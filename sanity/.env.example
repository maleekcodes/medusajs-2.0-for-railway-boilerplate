# Copy to `.env` or `.env.local` in this directory (Sanity CLI loads them for `pnpm dev`).
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Required only for `pnpm seed` (write access).
SANITY_TOKEN=
